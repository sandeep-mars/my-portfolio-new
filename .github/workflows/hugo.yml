steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive  # Fetches the theme code
          fetch-depth: 0

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: 'latest'
          extended: true         # Required for the Terminal theme
